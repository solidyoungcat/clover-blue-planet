[build]
builder = "nixpacks"
buildCommand = "npm install -g pnpm@8 && pnpm install --filter server && cd packages/server && npx tsc"

[deploy]
startCommand = "node packages/server/dist/index.js"
healthcheckPath = "/health"
restartPolicyType = "always"
